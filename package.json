{
  "name": "puter-service",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd puter-js && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
